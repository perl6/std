#!/usr/local/bin/perl

my $file = shift;

use STD;
use utf8;
use YAML::XS;
use Encode;

print STDERR "Starting...\n";
my $what = 'comp_unit';
my $text = Encode::decode('utf8', `cat $file`);
my $r = Perl->new($text)->$what();
print Dump($r);
