#!/usr/local/bin/perl

BEGIN { $ENV{STD5DEBUG} = 16383; }
my $file = shift;

use STD5;
use utf8;
use YAML::XS;
use Encode;

print "Starting...\n";
my $what = 'comp_unit';
my $text = Encode::decode('utf8', "1   ");
my $r = Perl->new($text)->$what();
print Dump($r);
