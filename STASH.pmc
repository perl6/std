use 5.010;
package STASH;
sub new {
    my $class = shift;
    my %attrs = @_;
    bless \%attrs, $class;
}
sub id    { return $_[0]->{'_id'} };
sub file  { return $_[0]->{'_file'} };
sub line  { return $_[0]->{'_line'} };
1;
