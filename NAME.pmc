use 5.010;
package NAME;
sub new {
    my $class = shift;
    my %attrs = @_;
    bless \%attrs, $class;
}
sub name  { my $self = shift; return $self->{name} };
sub file  { my $self = shift; return $self->{file} };
sub line  { my $self = shift; return $self->{line} };
sub xpad  { my $self = shift; return $self->{xpad} };
sub opad  { my $self = shift; return $self->{opad} };
1;
